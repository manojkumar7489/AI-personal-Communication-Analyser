[build]
  publish = "frontend"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[dev]
  publish = "frontend"
  port = 8888

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
  force = true